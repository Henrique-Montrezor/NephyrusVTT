# Build / empacotamento (Python + web + Electron)
build/
dist/
backend-dist/
release/
node_modules/

# Logs temporários
*.log

# Specs do PyInstaller são geradas, exceto a nossa (fonte).
*.spec
!nephyrus-host.spec